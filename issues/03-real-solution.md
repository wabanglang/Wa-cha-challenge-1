# [SOLUTION] REAL v0.4 — Observe → Hypothesize → Probe → Measure → Adapt → Learn

**Owner / originating author:** DOUGLAS W. T., JACKS0N

Independent Evidence Pass #4 broke one-shot causal `DIAGNOSE`. Structured assessment can identify observable/reported needs, but causal dominance, inferential reliability, predictive validity, and fairness are insufficient to treat an intake label as causal truth.

## `DEF:REAL.v0.4`

```text
REAL.v0.4 := O → H? → P → Δ → M → L

O := OBSERVE measurable/reported constraints, assets, preferences, risks
H? := HYPOTHESIZE candidate bottleneck(s) + confidence + alternatives + UNKNOWN
P := PROBE with smallest safe/reversible mechanism-matched intervention
Δ := MEASURE preregistered response + burden + harm + cost + persistence signal
M := KEEP | STOP | SWITCH | AUGMENT | DE-ESCALATE
L := LEARN/update hypothesis + decision rule
```

Optional modules remain `R?{RECONNECT}`, `E?{ENABLE}`, and `A?{ANCHOR}`, but they are candidate interventions—not conclusions generated by a score.

## `DEF:BOTTLENECK-HYPOTHESIS`

```text
{
  observable evidence,
  candidate mechanism,
  competing explanations,
  confidence,
  UNKNOWN,
  minimal probe,
  target outcome,
  response threshold,
  stop/harm rule,
  revision rule
}
```

## Evidence-repaired diagnosis claims

```text
D0 one-shot causal bottleneck diagnosis = BROKEN
D1 structured observable-need detection = FEASIBLE / TOOL-DEPENDENT
D2 causal dominance from intake assessment = NOT ESTABLISHED
D3 inter-rater agreement on inferential formulation = VARIABLE
D4 structured rules improve consistency = CONTEXT-DEPENDENT
D5 response-based adaptive testing = SUPPORTED METHODOLOGY
```

## Laws

```text
OBSERVE-BEFORE-INFER
UNKNOWN-IS-VALID
NO-SCORE-TO-CAUSALITY
NO-SCORE-TO-ALLOCATION
HYPOTHESIS-NOT-DIAGNOSIS
PROBE-MINIMUM
PREDECLARE-DECISION-RULE
NO-POSTHOC-RESCUE
NO-STACK-PREMIUM
MINIMUM-SUFFICIENT-FIRST
MARGINAL-COMPONENT-TEST
REMOVE-NONVALUE
ANCHOR-NOT-IDOL
```

## Next falsification target

`P := PROBE`: determine whether response to a short/minimal intervention supports causal updating or merely indicates a useful treatment response under uncertainty.

Evidence trail: Issue #3, Independent Evidence Passes #1–#4.